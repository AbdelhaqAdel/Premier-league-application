import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MatchDayResault extends StatelessWidget {
  const MatchDayResault({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child:
        Center(
          child:
          Text('Match Day result'),
        ),),
    );
  }
}